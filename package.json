{
  "name": "tosend-docs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "pnpm build && wrangler pages deploy .vitepress/dist",
    "deploy:prod": "pnpm build && wrangler pages deploy .vitepress/dist --branch=master"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vitepress": "1.6.4",
    "wrangler": "^4.61.1"
  }
}
